import React from 'react';

const StaticComponent = () => {
  return (
    <div>
      <h1>Hello, this is static content!</h1>
      <p>This paragraph and heading will always be displayed exactly like this when StaticComponent is used.</p>
    </div>
  );
};

export default StaticComponent;